# Override only when the backend lives somewhere other than the Vite dev proxy.
# Local dev: leave unset; the proxy in vite.config.ts forwards /api -> :8000.
# Production: set this to your deployed backend URL, e.g. https://api.example.com
VITE_API_BASE_URL=
